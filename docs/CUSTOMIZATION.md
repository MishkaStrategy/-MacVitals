# Customization

v1 provides combined menu bar presets (`Minimal`, `Performance`, `Power`, `Battery`, `Everything`) and drag-to-reorder enabled metrics in Preferences. The status item remains one compact native item for stability.

Separate status items and arbitrary SVG/PDF icon import are not enabled in the initial implementation. They require additional lifecycle, validation and hardware regression work and must not be represented as completed features.
