name: Physical Preview HUD Tile Editor Stacked

on:
  workflow_dispatch:

jobs:
  completed:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Physical preview capture completed; workflow retained only as historical evidence.'
