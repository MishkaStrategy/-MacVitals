name: Pull Request Preview Branch Disabled

on:
  pull_request:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: [self-hosted, macOS, ARM64]
    steps:
      - run: echo "Product CI already passed on exact head 82e95a608fcedb0325dee0a11ed93cc7d3156f84"
