name: Physical Direct-Session Validation (disabled)

# GitHub Actions runner usage is intentionally disabled for MacVitals.
# Build, test, packaging, security and physical validation must run locally on the Mac.
on:
  workflow_dispatch:

permissions: {}

jobs:
  github-actions-disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "GitHub Actions is disabled for MacVitals"
