name: Apply Power-Aware Sampling Intervals

on:
  push:
    branches: [feature/power-aware-sampling-intervals]
    paths:
      - .github/workflows/one-shot-power-aware-sampling.yml

permissions:
  contents: write

concurrency:
  group: one-shot-power-aware-sampling
  cancel-in-progress: true

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      - name: Patch source, localization, and tests
        shell: bash
        run: |
          set -Eeuo pipefail
          workdir="${RUNNER_TEMP}/macvitals-power-aware-sampling"
          rm -rf -- "${workdir}"
          git clone --depth=1 --branch "${GITHUB_REF_NAME}" \
            "https://x-access-token:${GH_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" \
            "${workdir}"
          cd "${workdir}"

          python3 - <<'PY'
          from pathlib import Path

          def replace_once(path: Path, old: str, new: str, label: str) -> None:
              text = path.read_text()
              count = text.count(old)
              if count != 1:
                  raise SystemExit(f"{label}: expected one anchor, found {count}")
              path.write_text(text.replace(old, new, 1))

          policy = Path("MacVitals/Monitoring/SamplingIntervalPolicy.swift")
          policy.write_text('''import Foundation

nonisolated enum SamplingPowerSource: String, Codable, Sendable {
  case battery
  case externalPower

  static func resolve(
    externalPowerConnected: Bool?,
    adapterConnected: Bool?,
    batteryPresent: Bool?,
    fallback: Self = .externalPower
  ) -> Self {
    if externalPowerConnected == true || adapterConnected == true || batteryPresent == false {
      return .externalPower
    }
    if externalPowerConnected == false {
      return .battery
    }
    return fallback
  }
}

nonisolated struct SamplingIntervalPreferences: Equatable, Sendable {
  let onBattery: TimeInterval
  let onExternalPower: TimeInterval

  init(onBattery: TimeInterval, onExternalPower: TimeInterval) {
    self.onBattery = SamplingIntervalPolicy.normalized(onBattery)
    self.onExternalPower = SamplingIntervalPolicy.normalized(onExternalPower)
  }

  static func resolve(
    legacyValue: TimeInterval?,
    batteryValue: TimeInterval?,
    externalPowerValue: TimeInterval?
  ) -> Self {
    let legacy = SamplingIntervalPolicy.normalized(
      legacyValue ?? SamplingIntervalPolicy.defaultValue)
    return Self(
      onBattery: batteryValue ?? legacy,
      onExternalPower: externalPowerValue ?? legacy)
  }

  func interval(for source: SamplingPowerSource) -> TimeInterval {
    switch source {
    case .battery: return onBattery
    case .externalPower: return onExternalPower
    }
  }
}

nonisolated enum SamplingIntervalPolicy {
  static let supportedValues: [TimeInterval] = [1, 2, 5, 10, 15, 30]
  static let defaultValue: TimeInterval = 5
  static let temperatureMinimumInterval: TimeInterval = 5
  static let historyDuration: TimeInterval = 60 * 60

  static func normalized(_ value: TimeInterval) -> TimeInterval {
    guard value.isFinite, value > 0 else { return defaultValue }
    return supportedValues.min { lhs, rhs in
      let lhsDistance = abs(lhs - value)
      let rhsDistance = abs(rhs - value)
      if lhsDistance == rhsDistance { return lhs < rhs }
      return lhsDistance < rhsDistance
    } ?? defaultValue
  }

  static func historyCapacity(for interval: TimeInterval) -> Int {
    let normalizedInterval = normalized(interval)
    let samples = Int(ceil(historyDuration / normalizedInterval))
    return max(120, min(3_600, samples))
  }

  static func sleepNanoseconds(
    intervalSeconds: TimeInterval,
    elapsedMilliseconds: Double
  ) -> UInt64 {
    let normalizedInterval = normalized(intervalSeconds)
    let delay = SamplingTimingMath.remainingDelaySeconds(
      intervalSeconds: normalizedInterval,
      elapsedMilliseconds: elapsedMilliseconds)
    guard delay.isFinite, delay > 0 else { return 0 }
    let nanoseconds = delay * 1_000_000_000
    guard nanoseconds.isFinite, nanoseconds > 0 else { return 0 }
    return UInt64(min(nanoseconds, Double(UInt64.max)))
  }
}
''')

          settings = Path("MacVitals/Persistence/SettingsStore.swift")
          replace_once(
              settings,
              '''  @Published var samplingInterval: Double {
    didSet {
      let normalized = SamplingIntervalPolicy.normalized(samplingInterval)
      if normalized != samplingInterval {
        samplingInterval = normalized
        return
      }
      UserDefaults.standard.set(normalized, forKey: Keys.samplingInterval)
    }
  }
''',
              '''  @Published var samplingIntervalOnBattery: Double {
    didSet {
      let normalized = SamplingIntervalPolicy.normalized(samplingIntervalOnBattery)
      if normalized != samplingIntervalOnBattery {
        samplingIntervalOnBattery = normalized
        return
      }
      UserDefaults.standard.set(normalized, forKey: Keys.samplingIntervalOnBattery)
    }
  }
  @Published var samplingIntervalOnExternalPower: Double {
    didSet {
      let normalized = SamplingIntervalPolicy.normalized(samplingIntervalOnExternalPower)
      if normalized != samplingIntervalOnExternalPower {
        samplingIntervalOnExternalPower = normalized
        return
      }
      let defaults = UserDefaults.standard
      defaults.set(normalized, forKey: Keys.samplingIntervalOnExternalPower)
      defaults.set(normalized, forKey: Keys.samplingInterval)
    }
  }
''',
              "SettingsStore sampling properties")

          replace_once(
              settings,
              '''    let defaults = UserDefaults.standard
    samplingInterval = SamplingIntervalPolicy.normalized(
      defaults.double(forKey: Keys.samplingInterval))
    showInDock = defaults.bool(forKey: Keys.showInDock)
''',
              '''    let defaults = UserDefaults.standard
    let samplingIntervals = SamplingIntervalPreferences.resolve(
      legacyValue: defaults.object(forKey: Keys.samplingInterval) == nil
        ? nil : defaults.double(forKey: Keys.samplingInterval),
      batteryValue: defaults.object(forKey: Keys.samplingIntervalOnBattery) == nil
        ? nil : defaults.double(forKey: Keys.samplingIntervalOnBattery),
      externalPowerValue: defaults.object(forKey: Keys.samplingIntervalOnExternalPower) == nil
        ? nil : defaults.double(forKey: Keys.samplingIntervalOnExternalPower))
    samplingIntervalOnBattery = samplingIntervals.onBattery
    samplingIntervalOnExternalPower = samplingIntervals.onExternalPower
    if defaults.object(forKey: Keys.samplingIntervalOnBattery) == nil {
      defaults.set(samplingIntervals.onBattery, forKey: Keys.samplingIntervalOnBattery)
    }
    if defaults.object(forKey: Keys.samplingIntervalOnExternalPower) == nil {
      defaults.set(samplingIntervals.onExternalPower, forKey: Keys.samplingIntervalOnExternalPower)
    }
    defaults.set(samplingIntervals.onExternalPower, forKey: Keys.samplingInterval)
    showInDock = defaults.bool(forKey: Keys.showInDock)
''',
              "SettingsStore sampling migration")

          replace_once(
              settings,
              '''    static let samplingInterval = "samplingInterval"
''',
              '''    static let samplingInterval = "samplingInterval"
    static let samplingIntervalOnBattery = "samplingInterval.onBattery"
    static let samplingIntervalOnExternalPower = "samplingInterval.onExternalPower"
''',
              "SettingsStore sampling keys")

          coordinator = Path("MacVitals/Monitoring/MetricsCoordinator.swift")
          replace_once(
              coordinator,
              '''  @Published private(set) var samplingHealth: SamplingHealth?
  @Published private(set) var isRunning = false
''',
              '''  @Published private(set) var samplingHealth: SamplingHealth?
  @Published private(set) var isRunning = false
  @Published private(set) var samplingPowerSource: SamplingPowerSource = .externalPower
  @Published private(set) var effectiveSamplingInterval: TimeInterval =
    SamplingIntervalPolicy.defaultValue
''',
              "MetricsCoordinator published sampling state")

          replace_once(
              coordinator,
              '''  private var fanBuffers: [Int: RingBuffer<TimedPoint>] = [:]
  private var samplingTask: Task<Void, Never>?
''',
              '''  private var fanBuffers: [Int: RingBuffer<TimedPoint>] = [:]
  private var samplingTask: Task<Void, Never>?
  private var samplingIntervals = SamplingIntervalPreferences(
    onBattery: SamplingIntervalPolicy.defaultValue,
    onExternalPower: SamplingIntervalPolicy.defaultValue)
''',
              "MetricsCoordinator sampling configuration storage")

          replace_once(
              coordinator,
              '''  func stop() {
    samplingTask?.cancel()
    samplingTask = nil
    isRunning = false
  }

  func handleSleep() {
''',
              '''  func stop() {
    samplingTask?.cancel()
    samplingTask = nil
    isRunning = false
  }

  func configureSamplingIntervals(
    onBattery: TimeInterval,
    onExternalPower: TimeInterval
  ) {
    samplingIntervals = SamplingIntervalPreferences(
      onBattery: onBattery,
      onExternalPower: onExternalPower)
    effectiveSamplingInterval = samplingIntervals.interval(for: samplingPowerSource)
  }

  func handleSleep() {
''',
              "MetricsCoordinator configure method")

          replace_once(
              coordinator,
              '''  private var currentInterval: TimeInterval {
    SamplingIntervalPolicy.normalized(
      UserDefaults.standard.double(forKey: "samplingInterval"))
  }

''',
              '''''',
              "MetricsCoordinator legacy interval lookup")

          replace_once(
              coordinator,
              '''        let interval = self?.currentInterval ?? SamplingIntervalPolicy.defaultValue
        self?.consume(result, configuredInterval: interval)
''',
              '''        let interval = self?.updateSamplingContext(from: result.snapshot)
          ?? SamplingIntervalPolicy.defaultValue
        self?.consume(result, configuredInterval: interval)
''',
              "MetricsCoordinator sampling loop interval")

          replace_once(
              coordinator,
              '''  private func consume(_ result: SampleResult, configuredInterval: TimeInterval) {
''',
              '''  private func updateSamplingContext(from snapshot: SystemSnapshot) -> TimeInterval {
    let source = SamplingPowerSource.resolve(
      externalPowerConnected: snapshot.battery.value?.externalPowerConnected,
      adapterConnected: snapshot.adapter.value?.connected,
      batteryPresent: snapshot.battery.value?.present,
      fallback: samplingPowerSource)
    samplingPowerSource = source
    effectiveSamplingInterval = samplingIntervals.interval(for: source)
    return effectiveSamplingInterval
  }

  private func consume(_ result: SampleResult, configuredInterval: TimeInterval) {
''',
              "MetricsCoordinator sampling context resolver")

          app = Path("MacVitals/App/AppDelegate.swift")
          replace_once(
              app,
              '''    NSApp.setActivationPolicy(settings.showInDock ? .regular : .accessory)
    statusController = StatusItemController(
''',
              '''    NSApp.setActivationPolicy(settings.showInDock ? .regular : .accessory)
    coordinator.configureSamplingIntervals(
      onBattery: settings.samplingIntervalOnBattery,
      onExternalPower: settings.samplingIntervalOnExternalPower)
    statusController = StatusItemController(
''',
              "AppDelegate initial sampling configuration")

          replace_once(
              app,
              '''    settings.$samplingInterval
      .removeDuplicates()
      .dropFirst()
      .sink { [weak self] interval in
        self?.consumptionHistory.restart(interval: interval)
      }
      .store(in: &cancellables)
''',
              '''    settings.$samplingIntervalOnBattery
      .combineLatest(settings.$samplingIntervalOnExternalPower)
      .dropFirst()
      .sink { [weak self] batteryInterval, externalPowerInterval in
        self?.coordinator.configureSamplingIntervals(
          onBattery: batteryInterval,
          onExternalPower: externalPowerInterval)
      }
      .store(in: &cancellables)

    coordinator.$effectiveSamplingInterval
      .removeDuplicates()
      .dropFirst()
      .sink { [weak self] interval in
        self?.consumptionHistory.restart(interval: interval)
      }
      .store(in: &cancellables)
''',
              "AppDelegate sampling subscriptions")

          replace_once(
              app,
              '''    consumptionHistory.start(interval: settings.samplingInterval)
''',
              '''    consumptionHistory.start(interval: coordinator.effectiveSamplingInterval)
''',
              "AppDelegate history start interval")

          preferences = Path("MacVitals/UI/Preferences/PreferencesView.swift")
          replace_once(
              preferences,
              '''        SettingsCard(
          title: L10n.string("Monitoring frequency"),
          subtitle: L10n.string("Choose how often live metrics and charts refresh."),
          symbol: "timer") {
            VStack(alignment: .leading, spacing: 10) {
              Picker("Update interval", selection: $settings.samplingInterval) {
                ForEach(SamplingIntervalPolicy.supportedValues, id: \.self) {
                  Text(L10n.format("%g s", $0)).tag($0)
                }
              }
              .labelsHidden()
              .pickerStyle(.segmented)
              .accessibilityIdentifier("samplingIntervalPicker")

              HStack(spacing: 7) {
                Image(systemName: settings.samplingInterval <= 2 ? "bolt.fill" : "leaf.fill")
                Text(samplingIntervalDescription)
              }
              .font(.caption)
              .foregroundStyle(.secondary)
            }
          }
''',
              '''        SettingsCard(
          title: L10n.string("Monitoring frequency"),
          subtitle: L10n.string("Choose separate refresh rates for battery and external power."),
          symbol: "timer") {
            VStack(alignment: .leading, spacing: 14) {
              samplingIntervalRow(
                title: L10n.string("On power adapter"),
                detail: samplingIntervalDescription(
                  for: settings.samplingIntervalOnExternalPower),
                symbol: "powerplug.fill",
                selection: $settings.samplingIntervalOnExternalPower,
                accessibilityIdentifier: "samplingIntervalExternalPowerPicker")

              Divider()

              samplingIntervalRow(
                title: L10n.string("On battery"),
                detail: samplingIntervalDescription(
                  for: settings.samplingIntervalOnBattery),
                symbol: "battery.75percent",
                selection: $settings.samplingIntervalOnBattery,
                accessibilityIdentifier: "samplingIntervalBatteryPicker")

              Divider()

              HStack(spacing: 8) {
                Image(systemName: coordinator.samplingPowerSource == .battery
                  ? "battery.75percent" : "powerplug.fill")
                  .foregroundStyle(Color.accentColor)
                VStack(alignment: .leading, spacing: 1) {
                  Text(L10n.string("Active profile"))
                    .font(.caption.weight(.semibold))
                  Text(
                    "\(samplingPowerSourceTitle) · "
                      + L10n.format("%g s", coordinator.effectiveSamplingInterval))
                    .font(.caption.monospacedDigit())
                    .foregroundStyle(.secondary)
                }
                Spacer()
                Text(L10n.string("Monitoring switches automatically when the power source changes."))
                  .font(.caption2)
                  .foregroundStyle(.secondary)
                  .multilineTextAlignment(.trailing)
                  .frame(maxWidth: 230, alignment: .trailing)
              }
            }
          }
''',
              "Preferences monitoring frequency card")

          replace_once(
              preferences,
              '''                value: L10n.format("%g s", settings.samplingInterval),
''',
              '''                value: L10n.format("%g s", coordinator.effectiveSamplingInterval),
''',
              "Preferences current interval")

          replace_once(
              preferences,
              '''  private var samplingIntervalDescription: String {
    switch settings.samplingInterval {
    case ...2: return L10n.string("Fastest response with higher sensor and energy overhead.")
    case ...5: return L10n.string("Balanced live monitoring for everyday use.")
    case ...15: return L10n.string("Lower overhead while keeping useful history.")
    default: return L10n.string("Lowest overhead for long-running monitoring.")
    }
  }

''',
              '''  private var samplingPowerSourceTitle: String {
    switch coordinator.samplingPowerSource {
    case .battery: return L10n.string("On battery")
    case .externalPower: return L10n.string("On power adapter")
    }
  }

  private func samplingIntervalDescription(for interval: TimeInterval) -> String {
    switch interval {
    case ...2: return L10n.string("Fastest response with higher sensor and energy overhead.")
    case ...5: return L10n.string("Balanced live monitoring for everyday use.")
    case ...15: return L10n.string("Lower overhead while keeping useful history.")
    default: return L10n.string("Lowest overhead for long-running monitoring.")
    }
  }

''',
              "Preferences sampling description")

          replace_once(
              preferences,
              '''  private func preferenceToggle(
''',
              '''  private func samplingIntervalRow(
    title: String,
    detail: String,
    symbol: String,
    selection: Binding<Double>,
    accessibilityIdentifier: String
  ) -> some View {
    VStack(alignment: .leading, spacing: 8) {
      HStack(spacing: 9) {
        Image(systemName: symbol)
          .font(.body.weight(.semibold))
          .foregroundStyle(Color.accentColor)
          .frame(width: 22)
        VStack(alignment: .leading, spacing: 1) {
          Text(title).font(.subheadline.weight(.semibold))
          Text(detail).font(.caption).foregroundStyle(.secondary)
        }
        Spacer()
        Text(L10n.format("%g s", selection.wrappedValue))
          .font(.body.monospacedDigit())
      }

      Picker("Update interval", selection: selection) {
        ForEach(SamplingIntervalPolicy.supportedValues, id: \.self) {
          Text(L10n.format("%g s", $0)).tag($0)
        }
      }
      .labelsHidden()
      .pickerStyle(.segmented)
      .accessibilityIdentifier(accessibilityIdentifier)
    }
  }

  private func preferenceToggle(
''',
              "Preferences sampling row helper")

          ui_tests = Path("MacVitalsUITests/MacVitalsUITests.swift")
          replace_once(
              ui_tests,
              '''    assertElementExists("samplingIntervalPicker", in: app, file: file, line: line)
''',
              '''    assertElementExists(
      "samplingIntervalExternalPowerPicker", in: app, file: file, line: line)
    assertElementExists("samplingIntervalBatteryPicker", in: app, file: file, line: line)
''',
              "UI test sampling identifiers")

          unit_tests = Path("MacVitalsTests/SamplingIntervalPolicyTests.swift")
          tests = unit_tests.read_text()
          insertion = '''
  func testLegacyIntervalMigratesToBothPowerProfiles() {
    let preferences = SamplingIntervalPreferences.resolve(
      legacyValue: 10,
      batteryValue: nil,
      externalPowerValue: nil)

    XCTAssertEqual(preferences.onBattery, 10)
    XCTAssertEqual(preferences.onExternalPower, 10)
  }

  func testPowerSpecificIntervalsOverrideLegacyValueIndependently() {
    let preferences = SamplingIntervalPreferences.resolve(
      legacyValue: 5,
      batteryValue: 15,
      externalPowerValue: 2)

    XCTAssertEqual(preferences.interval(for: .battery), 15)
    XCTAssertEqual(preferences.interval(for: .externalPower), 2)
  }

  func testPowerSpecificIntervalsNormalizeCorruptValues() {
    let preferences = SamplingIntervalPreferences.resolve(
      legacyValue: 5,
      batteryValue: .infinity,
      externalPowerValue: 7)

    XCTAssertEqual(preferences.onBattery, SamplingIntervalPolicy.defaultValue)
    XCTAssertEqual(preferences.onExternalPower, 5)
  }

  func testPowerSourceResolutionPrefersExternalPowerSignals() {
    XCTAssertEqual(
      SamplingPowerSource.resolve(
        externalPowerConnected: true,
        adapterConnected: false,
        batteryPresent: true,
        fallback: .battery),
      .externalPower)
    XCTAssertEqual(
      SamplingPowerSource.resolve(
        externalPowerConnected: nil,
        adapterConnected: true,
        batteryPresent: true,
        fallback: .battery),
      .externalPower)
    XCTAssertEqual(
      SamplingPowerSource.resolve(
        externalPowerConnected: nil,
        adapterConnected: nil,
        batteryPresent: false,
        fallback: .battery),
      .externalPower)
  }

  func testPowerSourceResolutionUsesBatteryAndStableFallback() {
    XCTAssertEqual(
      SamplingPowerSource.resolve(
        externalPowerConnected: false,
        adapterConnected: false,
        batteryPresent: true,
        fallback: .externalPower),
      .battery)
    XCTAssertEqual(
      SamplingPowerSource.resolve(
        externalPowerConnected: nil,
        adapterConnected: nil,
        batteryPresent: nil,
        fallback: .battery),
      .battery)
  }
'''
          closing = "\n}\n"
          if not tests.endswith(closing):
              raise SystemExit("SamplingIntervalPolicyTests closing anchor missing")
          unit_tests.write_text(tests[:-len(closing)] + insertion + closing)

          localization_entries = {
              Path("MacVitals/Resources/en.lproj/Localizable.strings"): '''"Choose separate refresh rates for battery and external power." = "Choose separate refresh rates for battery and external power.";
"On power adapter" = "On power adapter";
"Active profile" = "Active profile";
"Monitoring switches automatically when the power source changes." = "Monitoring switches automatically when the power source changes.";
''',
              Path("MacVitals/Resources/ru.lproj/Localizable.strings"): '''"Choose separate refresh rates for battery and external power." = "Задайте отдельную частоту обновления для батареи и внешнего питания.";
"On power adapter" = "От сети";
"Active profile" = "Активный профиль";
"Monitoring switches automatically when the power source changes." = "Интервал переключается автоматически при смене источника питания.";
''',
          }
          for path, entries in localization_entries.items():
              text = path.read_text()
              anchor = '"Update interval" = '
              if text.count(anchor) != 1:
                  raise SystemExit(f"{path}: update interval localization anchor mismatch")
              line_end = text.index("\n", text.index(anchor)) + 1
              path.write_text(text[:line_end] + entries + text[line_end:])

          workflow = Path(".github/workflows/one-shot-power-aware-sampling.yml")
          workflow.unlink()
          PY

          echo "Remaining sampling interval references:"
          git grep -n "samplingInterval" -- MacVitals MacVitalsTests MacVitalsUITests || true

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add \
            MacVitals/Monitoring/SamplingIntervalPolicy.swift \
            MacVitals/Persistence/SettingsStore.swift \
            MacVitals/Monitoring/MetricsCoordinator.swift \
            MacVitals/App/AppDelegate.swift \
            MacVitals/UI/Preferences/PreferencesView.swift \
            MacVitals/Resources/en.lproj/Localizable.strings \
            MacVitals/Resources/ru.lproj/Localizable.strings \
            MacVitalsTests/SamplingIntervalPolicyTests.swift \
            MacVitalsUITests/MacVitalsUITests.swift \
            .github/workflows/one-shot-power-aware-sampling.yml
          git commit -m "feat: add power-aware sampling intervals"
          git push origin "HEAD:${GITHUB_REF_NAME}"
