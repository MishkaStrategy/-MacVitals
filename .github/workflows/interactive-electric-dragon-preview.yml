name: Interactive Electric Dragon Preview Disabled

on:
  pull_request:
    branches: [agent/status-bar-electric-dragon]
    types: [opened, synchronize, reopened]

jobs:
  disabled:
    if: ${{ false }}
    runs-on: [self-hosted, macOS, ARM64]
    steps:
      - run: echo disabled
