# Contributing

Use Xcode 16+, Swift 6 and macOS 13+. Keep providers read-only, capability checked and explicit about source/quality. Add deterministic unit tests for calculations and do not claim hardware coverage without evidence.

Run `make test` and `make lint` before opening a pull request. Native builds and tests belong on a Mac runner; Linux checks are limited to repository validation, documentation, licenses and secret scanning.
